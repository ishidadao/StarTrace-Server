# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [1.1.0] - 2026-08-24

### 新增

- 新增账号级完整 JSON 备份下载，覆盖全部原神、鸣潮和游戏 UID；备份文件可在星迹中直接重新导入。
- 新增数字、保底圆环、稀有度圆环、趋势柱形和页面切换动画，并支持系统“减少动态效果”偏好。

### 安全

- 登录与注册请求增加服务端类型、字符集、密码长度及 4 KiB 请求体限制。
- 登录查询继续使用参数绑定，并对不存在的用户执行固定成本密码校验，减弱账号枚举的时序差异。
- 下载备份不包含密码、会话令牌、Cookie、原始上传内容或服务器内部判定字段。

### 修复

- 统一保底环和稀有度环的起点为 12 点钟方向。
- 切换游戏、UID 或卡池时先完成数据载入再切换视图，减少旧数据短暂显示在新账号下的问题。

## [1.0.1] - 2026-08-24

### 修复

- 保留鸣潮接口返回的同秒十连内部顺序，修复五星位于十连中间时当前垫抽数偏大的问题。
- 让抽卡活跃趋势的柱形在可用横向区域内均匀分布，消除图表右侧的大块空白。

### 数据库

- 为 `gacha_records` 增加 `draw_order` 字段。应用启动时会自动完成兼容迁移；早期版本的鸣潮记录应重新同步一次以补齐顺序。
